{
    "init": true, // Avoid leaving zombie processes.
    "privileged": true, // Docker in Docker requires privileged mode, otherwise use sysbox runtime.
    "overrideCommand": false, // Avoid messing with our container command. Docker daemon won't start if this is true (default).
    "image": "charlie0129/devpods:ubuntu24.04-docker28.3.3-golang1.25.1-python3.12-uv0.8.15"
}